namespace: ibm-spectrum-scale

commonLabels:
  app.kubernetes.io/instance: ibm-spectrum-scale
  app.kubernetes.io/name: cluster

resources:
  - scale_v1beta1_encryptionconfig.yaml
# +kubebuilder:scaffold:manifestskustomizesamples
